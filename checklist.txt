# React + Tauri port checklist

`[x]` means the feature is implemented and verified in the new `main` application.

- [x] Preserve the Qt application under `legacy/`.
- [x] Use the supplied `web/` React UI as the visual source of truth in `main/`.
- [x] Create the Tauri 2 native host and typed command bridge.
- [x] Build and bundle the upstream `sq1opt.cpp` native solver.
- [x] Render and edit Square-1 state, including piece swaps and partial pieces.
- [x] Support U/U′/D/D′/slice, mouse controls, and keyboard shortcuts.
- [x] Port the 64-step undo/redo behavior for mouse edits, controls, input application, reset, and grouped slices.
- [x] Port complete position parsing, including the exact U/V/W/X/Y/Z identity assignment.
- [x] Port full Karnotation and alignment-dependent shorthand parsing through the shared legacy C++ implementation.
- [x] Support numeric scramble and inverse-algorithm application with blocked-slice validation.
- [x] Port the legacy option constraints, depth validation, normalize-ABF behavior, and command construction/preview.
- [x] Run the embedded `sq1opt.cpp` solver off the UI thread, stream status through a Tauri channel, and support stopping it.
- [x] Port solution deduplication, Karn/Abid conversion, table/terminal modes, and persistent favorites.
- [x] Port the shared cubeshape ergonomic ratings, median normalization, and rank sorting.
- [x] Persist the legacy run/display settings configuration and restore it on startup.
- [x] Port the Settings modal and copy/table/expand output-toolbar behavior.
- [x] Add a cross-platform build entrypoint for Windows, Linux, macOS, and Android.
- [x] Type-check and production-build the web frontend.
